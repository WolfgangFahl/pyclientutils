{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"]},"docs":[{"location":"","title":"pyclientutils API Documentation","text":""},{"location":"#clientutils.cmd","title":"<code>cmd</code>","text":"<p>Command line entry point</p>"},{"location":"#clientutils.cmd.ClientUtilsCmd","title":"<code>ClientUtilsCmd</code>","text":"<p>               Bases: <code>BaseCmd</code></p> <p>Command Line Interface</p> Source code in <code>clientutils/cmd.py</code> <pre><code>class ClientUtilsCmd(BaseCmd):\n    \"\"\"Command Line Interface\"\"\"\n\n    def getArgParser(self, description: str, version_msg) -&gt; ArgumentParser:\n        \"\"\"get the argument parser\"\"\"\n        parser = super().getArgParser(description, version_msg)\n        parser.add_argument(\n            \"--start\",\n            action=\"store_true\",\n            help=\"start the webserver\",\n        )\n        parser.add_argument(\n            \"--port\",\n            dest=\"port\",\n            type=int,\n            default=9998,\n            help=\"port for the webserver (default: 9998)\",\n        )\n        return parser\n\n    def handle_args(self, args: Namespace) -&gt; bool:\n        \"\"\"Handle the parsed arguments\"\"\"\n        # Let base class handle standard args (--about, --debug, etc.)\n        handled = super().handle_args(args)\n        if handled:\n            return True\n\n        # Now handle our custom args\n        if args.start:\n            server = ClientUtilsServer(port=args.port)\n            print(f\"Starting ClientUtils server on port {args.port}...\")\n            server.start()\n            return True  # Signal we handled it\n\n        # If --start not provided, just exit normally\n        return False\n</code></pre>"},{"location":"#clientutils.cmd.ClientUtilsCmd.getArgParser","title":"<code>getArgParser(description, version_msg)</code>","text":"<p>get the argument parser</p> Source code in <code>clientutils/cmd.py</code> <pre><code>def getArgParser(self, description: str, version_msg) -&gt; ArgumentParser:\n    \"\"\"get the argument parser\"\"\"\n    parser = super().getArgParser(description, version_msg)\n    parser.add_argument(\n        \"--start\",\n        action=\"store_true\",\n        help=\"start the webserver\",\n    )\n    parser.add_argument(\n        \"--port\",\n        dest=\"port\",\n        type=int,\n        default=9998,\n        help=\"port for the webserver (default: 9998)\",\n    )\n    return parser\n</code></pre>"},{"location":"#clientutils.cmd.ClientUtilsCmd.handle_args","title":"<code>handle_args(args)</code>","text":"<p>Handle the parsed arguments</p> Source code in <code>clientutils/cmd.py</code> <pre><code>def handle_args(self, args: Namespace) -&gt; bool:\n    \"\"\"Handle the parsed arguments\"\"\"\n    # Let base class handle standard args (--about, --debug, etc.)\n    handled = super().handle_args(args)\n    if handled:\n        return True\n\n    # Now handle our custom args\n    if args.start:\n        server = ClientUtilsServer(port=args.port)\n        print(f\"Starting ClientUtils server on port {args.port}...\")\n        server.start()\n        return True  # Signal we handled it\n\n    # If --start not provided, just exit normally\n    return False\n</code></pre>"},{"location":"#clientutils.cmd.Version","title":"<code>Version</code>","text":"<p>Version information</p> Source code in <code>clientutils/cmd.py</code> <pre><code>class Version:\n    \"\"\"Version information\"\"\"\n\n    name = \"clientutils\"\n    version = clientutils.__version__\n    description = \"MediaWiki Client Utilties\"\n    doc_url = \"https://media.bitplan.com/index.php?title=CPSA-A-Analysis\"\n    updated = \"2026-01-28\"\n</code></pre>"},{"location":"#clientutils.cmd.main","title":"<code>main(argv=None)</code>","text":"<p>Main entry point.</p> Source code in <code>clientutils/cmd.py</code> <pre><code>def main(argv=None):\n    \"\"\"Main entry point.\"\"\"\n    exit_code = ClientUtilsCmd.main(Version(), argv)\n    return exit_code\n</code></pre>"},{"location":"#clientutils.webserver","title":"<code>webserver</code>","text":"<p>Simple REST server for serving file icons</p>"},{"location":"#clientutils.webserver.ClientUtilsServer","title":"<code>ClientUtilsServer</code>","text":"<p>Serves static file icons via HTTP</p> Source code in <code>clientutils/webserver.py</code> <pre><code>class ClientUtilsServer:\n    \"\"\"Serves static file icons via HTTP\"\"\"\n\n    def __init__(self, port=9998):\n        self.port = port\n        self.app = Flask(__name__)\n        self._setup_routes()\n\n    def get_icons_directory(self) -&gt; Path:\n        \"\"\"\n        Get the path to the icons directory.\n\n        Returns:\n            Path: Absolute path to the icons directory\n\n        Raises:\n            FileNotFoundError: If icons directory doesn't exist\n        \"\"\"\n        # Try relative to this file\n        icons_dir = Path(__file__).parent.parent / \"clientutils_examples\" / \"icons\"\n\n        if not icons_dir.exists():\n            # Try relative to current working directory\n            icons_dir = Path.cwd() / \"clientutils_examples\" / \"icons\"\n\n        if not icons_dir.exists():\n            raise FileNotFoundError(f\"Icons directory not found. Tried: {icons_dir}\")\n\n        return icons_dir.resolve()\n\n    def _setup_routes(self):\n        \"\"\"Configure routes for static file serving\"\"\"\n        # Get the icons directory path\n        icons_dir = self.get_icons_directory()\n\n        @self.app.route(\"/fileicon/&lt;path:filename&gt;\")\n        def serve_icon(filename):\n            \"\"\"Serve file icons\"\"\"\n            return send_from_directory(icons_dir, filename)\n\n    def start(self):\n        \"\"\"Start the web server\"\"\"\n        self.app.run(host=\"0.0.0.0\", port=self.port, debug=False)\n</code></pre>"},{"location":"#clientutils.webserver.ClientUtilsServer.get_icons_directory","title":"<code>get_icons_directory()</code>","text":"<p>Get the path to the icons directory.</p> <p>Returns:</p> Name Type Description <code>Path</code> <code>Path</code> <p>Absolute path to the icons directory</p> <p>Raises:</p> Type Description <code>FileNotFoundError</code> <p>If icons directory doesn't exist</p> Source code in <code>clientutils/webserver.py</code> <pre><code>def get_icons_directory(self) -&gt; Path:\n    \"\"\"\n    Get the path to the icons directory.\n\n    Returns:\n        Path: Absolute path to the icons directory\n\n    Raises:\n        FileNotFoundError: If icons directory doesn't exist\n    \"\"\"\n    # Try relative to this file\n    icons_dir = Path(__file__).parent.parent / \"clientutils_examples\" / \"icons\"\n\n    if not icons_dir.exists():\n        # Try relative to current working directory\n        icons_dir = Path.cwd() / \"clientutils_examples\" / \"icons\"\n\n    if not icons_dir.exists():\n        raise FileNotFoundError(f\"Icons directory not found. Tried: {icons_dir}\")\n\n    return icons_dir.resolve()\n</code></pre>"},{"location":"#clientutils.webserver.ClientUtilsServer.start","title":"<code>start()</code>","text":"<p>Start the web server</p> Source code in <code>clientutils/webserver.py</code> <pre><code>def start(self):\n    \"\"\"Start the web server\"\"\"\n    self.app.run(host=\"0.0.0.0\", port=self.port, debug=False)\n</code></pre>"}]}